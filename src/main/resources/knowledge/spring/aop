AOP 的工作重心在于如何将增强应用于目标对象的连接点上。这里包含两项工作：
　　1、如何通过 切点 和 增强 定位到 连接点 上。
　　2、如何在 增强 中编写切面的代码。

AOP有三种织入方式：
编译期织入，这要求使用特殊的java编译器。
类加载时织入，这要求使用特殊的类加载器。
动态代理织入，在运行期为目标类添加 增强 生成 子类 的方式。动态代理有两种，jdk动态代理（基于接口来实现）、CGLib（基于类来实现）